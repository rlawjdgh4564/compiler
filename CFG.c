#include <stdio.h>

int main(){
	char* func[8] = {"if", "else", "else if", "goto", "for", "while", "do", "switch"};
	int i;
	FILE *fd = fopen("input.txt". "r");
	int block_num = 0;

	if( fd != NULL)
	{
		char strTmp[255];
		char *pStr;
		while()
	}
	return 0;
}
